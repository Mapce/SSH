<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
 http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-3.1.xsd">

    <context:annotation-config></context:annotation-config>
    <context:property-placeholder location="WEB-INF/resources/common-properties"></context:property-placeholder>
<context:component-scan base-package="yeying"></context:component-scan>
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <!--配置c3p0链接参数-->
<property name="driverClass" value="${jdbc-driverClass}"></property>
        <property name="user" value="${jdbc-username}"></property>
        <property name="password" value="${jdbc-password}"></property>
        <property name="jdbcUrl" value="${jdbc-url}"></property>
        <property name="initialPoolSize" value="${jdbc-initPoolSize}"></property>
        <property name="minPoolSize" value="${jdbc-minPoolSize}"></property>
        <property name="maxPoolSize" value="${jdbc-maxPoolSize}"></property>
        <property name="maxIdleTime" value="${jdbc-maxIdelTime}" />
    </bean>
    <!--hibernate sessionFactory配置-->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
<property name="dataSource" ref="dataSource"></property>
    <property name="packagesToScan" >
        <list>
            <value>yeying.entity</value>
        </list>
    </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${hibernate-dialect}</prop>
                <prop key="hibernate.show_sql">${hibernate-show-sql}</prop>
                <prop key="hibernate.format_sql">${hibernate-format_sql}</prop>
                <prop key="hibernate.use_sql_commants">${hibernate-comments}</prop>
                <prop key="hibernate.hbm2ddl.auto">${hibernate-hbm2ddl.auto}</prop>
            </props>
        </property>
</bean>

    <!--//hibernateTemplate-->
    <bean id="hibernateTemplate" class="org.springframework.orm.hibernate4.HibernateTemplate">
        <property name="sessionFactory" ref="sessionFactory"></property>
    </bean>

</beans>